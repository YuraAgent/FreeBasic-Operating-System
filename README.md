Little change:

A missmatch code in FreeBasic related with support menu bar and little error clicking button mouse.

"Fragment's code:
  if mb = 0 then
  elseif mb = 1 then
      if MenuX > 8 then X = 0
      if MenuX < 0 then X = 8
      if MenuY > 2 then Y = 0
      if MenuY < 0 then Y = 2
      Button_tag(MenuX + 8,MenuY + 2)
  end if
"
Exactly i must it change code right now.

t.me\@SkyFerest_Channel
x.com\@SkyFerest17
